# Database (SQLite path or PostgreSQL connection string)
DATABASE_URL=file:./data/namedrop.db

# Auth secret for encrypting registrar API keys
AUTH_SECRET=changeme

# Password protection (optional — leave empty to disable)
# AUTH_PASSWORD=your-password-here

# Port
PORT=3000
